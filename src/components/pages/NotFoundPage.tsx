import { MainContent } from '../MainContent.ts';
import { Header } from '../Header/Header.tsx';
import { Footer } from '../Footer/Footer.tsx';

export const NotFoundPage = () => (
  <>
    <Header />
    <MainContent>Not found page placeholder.</MainContent>
    <Footer />
  </>
);
